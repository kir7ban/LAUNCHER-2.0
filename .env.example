# Backend Configuration
# Set to 'true' to use real backend; 'false' for mock responses
VITE_USE_REAL_BACKEND=false

# Backend URL (WebSocket is derived by replacing http with ws)
VITE_BACKEND_URL=http://localhost:8080
